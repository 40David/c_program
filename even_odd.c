#include<stdio.h>
int main()
{
    int n;
    printf("Enter a number: ");
    scanf("%d", &n);
    if (n%2==0)
    printf("the numner is even ");
    else
    printf("the number is odd");

return 0;
}